#include <stdio.h>
#include <inttypes.h>

int main(void)
{
    int c = 0;
    char ch;
    int16_t n;

    //typedef();

    printf("Hello World!\n");

    for(c = 8; c < 8; c++){
        ch = c;
        printf("%d: " "%c\n", ch, ch);
        //7 - звук
        n = 100;
    }

    //limits.h и float.h
    printf("%u", sizeof(long));
    return 0;
}

